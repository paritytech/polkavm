cargo-features = ["edition2024"]

[package]
name = "blake2b"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
name = "blake2b"
path = "src/main.rs"
crate-type = ["staticlib", "rlib"]

[dependencies]
polkavm-derive = { path = "../../crates/polkavm-derive" }
utils = { path = "../utils" }
